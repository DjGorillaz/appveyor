image:
  - Visual Studio 2017

clone_folder: c:\project
shallow_clone: true
matrix:
  fast_finish: true

init:
  - set PATH=C:\Qt\5.11.0\%qt%\bin;%PATH%
  - set ORIGPATH=%PATH%
 
#install:

before_build:
    - cd c:\project\
    - md build
    - cd build

build_script:
  - cmake .. -G "Visual Studio 15 2017 Win64"
  - cmake --build . --config Release

after_build:
  - cd c:\project\
  - 7z a appveyor.zip %APPVEYOR_BUILD_FOLDER%\Release\*

test: off

artifacts:
  - path: appveyor.zip
    name: artifacts
    
deploy:
  provider: GitHub
  tag: appveyor-v$(appveyor_build_version)
  description: App for windows
  auth_token:
    secure: RHuRwIcFSQ7rw2VLEAaqtzcPiviAnV4jtOibhY3EJF6HfleGKoLWOXbfxs/N9XJq
  artifact: artifacts.zip